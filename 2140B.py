for _ in range(int(input())):
    x = int(input())


    """
    (x # y) % (x + y) == 0

    what if y = 2x ? 
    (x + 2x ) % (3x) == 0 

    in problems like these 
    1. try to convert on variable into other
    2. assume some kind of relation
    """

    print(2*x)