# UNSOLVED
for _ in range(int(input())):
    n = int(input())
    arr = list(map(int, input().split()))

    # array contains only 0 and 1
    
    # choose two indices i, j
    # add ai to aj
    # remove ai 

    # minmum number of operations to make the array non-decreasing.